//! WASM bindings.
//!
//! TODO(P4, plan 66): wasm-bindgen `#[wasm_bindgen]` surface over
//! [`crate::ffi::shared`] batch calls, published as `@kotoshu/wasm`. The
//! `wasm` feature is declared in Cargo.toml without its wasm-bindgen
//! dependency until then.

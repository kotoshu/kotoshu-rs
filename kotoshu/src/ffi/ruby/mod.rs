//! Ruby bindings.
//!
//! TODO(P4, plan 66): magnus `init` exposing the engine over
//! [`crate::ffi::shared`] batch calls, `[patch.crates-io]` rb-sys revs per
//! parsanol policy. The `ruby` feature is declared in Cargo.toml without its
//! magnus dependency until then.

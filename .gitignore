# Generated by Cargo
/target/

# Build artifacts
*.rlib
*.rmeta
*.so
*.dylib
*.dll

# IDE and editor files
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Coverage / profiling
/coverage/
*.lcov
*.profraw
*.profdata
flamegraph.svg
perf.data

# Conformance fixture dictionaries — synced from the gem repo by
# scripts/sync_conformance.sh, never committed (licensing stays with the
# gem repo, which documents provenance).
/tests/fixtures/

# Claude Code instruction files - NEVER commit these
CLAUDE.md
CLAUDE.local.md

# Ruby-feature smoke shim (tests/ruby_ext) is excluded from the workspace;
# its lock file is a test artifact (the pinned core lock lives at the root).
/tests/ruby_ext/Cargo.lock
/tests/ruby_ext/target/

# wasm-pack output for @kotoshu/wasm — a build artifact, never committed
# (see kotoshu-wasm/RELEASING.md for the blocked publish)
/kotoshu-wasm/pkg/
